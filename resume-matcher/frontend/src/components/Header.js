import React from 'react';

const Header = () => {
  return (
    <header className="header">
      <h1>Smart Resume Matcher</h1>
      <p>Find the perfect candidates with regex extraction and semantic search</p>
    </header>
  );
};

export default Header;
